spring:
  application:
    name: chat-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:in-memory}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

katsubot:
  rag:
    # 외부 RAG 서비스 (별도 배포). 로컬: infra/dummy-rag 스텁
    base-url: ${RAG_SERVICE_BASE_URL:http://localhost:8090}
  auth:
    dev-bypass: ${KATSUBOT_AUTH_DEV_BYPASS:true}
    dev-token: ${KATSUBOT_AUTH_DEV_TOKEN:dev-token}
    jwt-secret: ${KATSUBOT_AUTH_JWT_SECRET:}
